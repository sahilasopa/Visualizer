package com.example.visualizer;

public class Test {
    // cast first one to string
    // try casting second one too but in case of string value do something else
    // now if first one is alreay there more then once, dont add key insted add key
}
